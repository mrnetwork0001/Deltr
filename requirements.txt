# Deltr backend — Python 3.11+ (install: pip install -r requirements.txt)
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
mcp>=1.29,<2

# dev / test
pytest>=8
pytest-asyncio>=0.23
